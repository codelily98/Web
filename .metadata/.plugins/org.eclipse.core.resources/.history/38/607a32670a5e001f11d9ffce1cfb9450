<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>문제 1</title>
</head>
<body>
	<h3>1 ~ 10 사이의 덧셈 문제를 발생합니다.</h3>
	<input type="button" value="문제생성" onclick="start()"/>
	<br>
	<div><span id="random_num">?</span>
	<input type="button" value="결과보기" onclick="printResult()"/></div>
	
<script type="text/javascript">
function sum(x, y) {
	return x + y;
}
function start() {
	var x = parseInt(Math.random() * 10 + 1);
	var y = parseInt(Math.random() * 10 + 1);
	var myTag = document.getElementById("random_num");
	myTag.innerHTML = x + " + " + y + " = " + "?";
	
	re_sum = sum(x, y);
	return re_sum;
}
function printResult() {	
	var myTag = document.getElementById("random_num");
	myTag.innerHTML = x + " + " + y + " = " + re_sum;
}
</script>
</body>
</html>